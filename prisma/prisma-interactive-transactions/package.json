{
  "scripts": {
    "postinstall": "npm run prisma:generate",
    "start": "tsx",
    "db:reset": "prisma db push && npm run prisma:generate",
    "prisma:generate": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^4.13.0"
  },
  "devDependencies": {
    "@types/node": "^18.15.13",
    "prisma": "^4.13.0",
    "tsx": "^3.12.6",
    "typescript": "^5.0.4"
  }
}
